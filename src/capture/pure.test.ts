import{describe,expect,it}from'vitest';import{buildIdempotencyKey}from'./deduplicate';import{extractMetric,normalizeVerdict}from'./normalize';
describe('capture pure functions',()=>{it('normalizes only configured terminal verdicts',()=>{expect(normalizeVerdict('Accepted',[[/^Accepted$/,'accepted']])).toBe('accepted');expect(normalizeVerdict('Judging',[[/^Accepted$/,'accepted']])).toBe('unknown')});it('extracts metrics',()=>expect(extractMetric('Runtime: 4 ms',/Runtime/)).toBe('4 ms'));it('deduplicates by five-second bucket',()=>{const base={platform:'luogu',problemKey:'P1',accountKey:'anonymous',language:'cpp',codeHash:'hash',submittedAt:10000};expect(buildIdempotencyKey(base)).toBe(buildIdempotencyKey({...base,submittedAt:14999}));expect(buildIdempotencyKey(base)).not.toBe(buildIdempotencyKey({...base,submittedAt:15000}))})});
