import{beforeEach,describe,expect,it}from'vitest';import type{SnapshotProvider}from'../types';import{createLeetCodeAdapter}from'./adapter';
const bridge=(code:string|null):SnapshotProvider=>({getEditorSnapshot:async()=>code?{code,language:'python'}:null});beforeEach(()=>{history.pushState({},'','/problems/two-sum/');document.body.innerHTML='<h1>两数之和</h1>'});describe('LeetCode adapter',()=>{it('matches problem pages and reads identity',async()=>{const adapter=createLeetCodeAdapter('leetcode-cn',{bridge:bridge(null)});expect(adapter.matchLocation(new URL('https://leetcode.cn/problems/two-sum/'))).toBe(true);expect(await adapter.getProblemIdentity()).toMatchObject({problemKey:'two-sum',title:'两数之和',accountKey:'anonymous'})});it('reads editor model first',async()=>expect(await createLeetCodeAdapter('leetcode-cn',{bridge:bridge('print(1)')}).readEditorSnapshot()).toMatchObject({code:'print(1)',method:'editor-model'}))});
