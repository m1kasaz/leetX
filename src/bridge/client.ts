import type{SnapshotProvider}from'../adapters/types'; import{MAX_CODE_LENGTH}from'../messaging/messages'; import{BRIDGE_CHANNEL,bridgeResponseSchema}from'./protocol';
export type BridgeClient=SnapshotProvider; export interface BridgeClientOptions{nonce?:string;timeoutMs?:number;maxCodeLength?:number}
export function createBridgeClient(options:BridgeClientOptions={}):SnapshotProvider{const nonce=options.nonce??document.documentElement.dataset.leetxNonce??'';const timeout=options.timeoutMs??1500;const max=options.maxCodeLength??MAX_CODE_LENGTH;return{getEditorSnapshot(){const requestId=crypto.randomUUID();return new Promise(resolve=>{let done=false;const finish=(value:{code:string;language:string}|null)=>{if(done)return;done=true;clearTimeout(timer);window.removeEventListener('message',onMessage);resolve(value)};const onMessage=(event:MessageEvent)=>{const parsed=bridgeResponseSchema.safeParse(event.data);if(!parsed.success)return;const r=parsed.data;if(r.nonce!==nonce||r.requestId!==requestId)return;if(!r.ok||!r.payload||r.payload.code.length>max)return finish(null);finish(r.payload)};const timer=setTimeout(()=>finish(null),timeout);window.addEventListener('message',onMessage);window.postMessage({source:'leetx-content',channel:BRIDGE_CHANNEL,nonce,requestId,action:'GET_EDITOR_SNAPSHOT'},location.origin);});}}}
