import{afterEach,describe,expect,it}from'vitest';import{createBridgeClient}from'./client';import{installPageBridge,readMainWorldSnapshot}from'./pageBridge';
afterEach(()=>{delete(window as unknown as{monaco?:unknown}).monaco;document.body.innerHTML=''});describe('MAIN world bridge',()=>{it('reads Monaco and roundtrips with nonce',async()=>{(window as unknown as{monaco:unknown}).monaco={editor:{getModels:()=>[{getValue:()=> 'print(42)',getLanguageId:()=> 'python'}]}};expect(readMainWorldSnapshot()).toEqual({code:'print(42)',language:'python'});installPageBridge('nonce-123456');await expect(createBridgeClient({nonce:'nonce-123456',timeoutMs:100}).getEditorSnapshot()).resolves.toEqual({code:'print(42)',language:'python'})});it('rejects a wrong nonce by timing out',async()=>{installPageBridge('nonce-123456');await expect(createBridgeClient({nonce:'other-123456',timeoutMs:10}).getEditorSnapshot()).resolves.toBeNull()})});
