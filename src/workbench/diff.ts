export type DiffKind='same'|'added'|'removed';export interface DiffLine{kind:DiffKind;text:string;oldLine?:number;newLine?:number}
export function diffLines(previous:string,current:string):DiffLine[]{const a=previous.split('\n'),b=current.split('\n');const dp=Array.from({length:a.length+1},()=>Array<number>(b.length+1).fill(0));for(let i=a.length-1;i>=0;i--)for(let j=b.length-1;j>=0;j--)dp[i]![j]=a[i]===b[j]?1+dp[i+1]![j+1]!:Math.max(dp[i+1]![j]!,dp[i]![j+1]!);const result:DiffLine[]=[];let i=0,j=0;while(i<a.length||j<b.length){if(i<a.length&&j<b.length&&a[i]===b[j]){result.push({kind:'same',text:a[i]!,oldLine:++i,newLine:++j})}else if(j<b.length&&(i===a.length||dp[i]![j+1]!>=dp[i+1]![j]!)){result.push({kind:'added',text:b[j]!,newLine:++j})}else{result.push({kind:'removed',text:a[i]!,oldLine:++i})}}return result}
